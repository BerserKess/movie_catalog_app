<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/overlay" /> <!-- Fundo levemente translúcido -->
    <corners android:radius="12dp" />         <!-- Cantos arredondados -->
    <stroke
        android:width="1dp"
        android:color="@color/color_secondary" /> <!-- Borda suave -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>